[
  "lavender-vanilla-oat-latte: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "cookies-and-cream-dream-frappe: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "salted-caramel-toffee-frappe: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "red-velvet-cheesecake-frappe: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "lavender-haze-latte: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "cotton-candy-cloud: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "caramel-apple-tini: no tags (no data-seasons/data-profiles)",
  "caramel-apple-tini: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "caramel-apple-tini: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "creme-brulee-white-mocha: no tags (no data-seasons/data-profiles)",
  "creme-brulee-white-mocha: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "creme-brulee-white-mocha: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "frozen-tiramisu-coffee: no tags (no data-seasons/data-profiles)",
  "frozen-tiramisu-coffee: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "frozen-tiramisu-coffee: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "pumpkin-pie-mimosa: no tags (no data-seasons/data-profiles)",
  "pumpkin-pie-mimosa: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "pumpkin-pie-mimosa: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "pumpkin-spice-dessert-liqueur: no tags (no data-seasons/data-profiles)",
  "pumpkin-spice-dessert-liqueur: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "pumpkin-spice-dessert-liqueur: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "brandied-fruit-smoothie: no tags (no data-seasons/data-profiles)",
  "brandied-fruit-smoothie: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "brandied-fruit-smoothie: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "caramel-apple-fizz: no tags (no data-seasons/data-profiles)",
  "caramel-apple-fizz: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "caramel-apple-fizz: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "white-chocolate-macadamia-latte: no tags (no data-seasons/data-profiles)",
  "white-chocolate-macadamia-latte: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "white-chocolate-macadamia-latte: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "pumpkin-spice-martini: no tags (no data-seasons/data-profiles)",
  "pumpkin-spice-martini: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "pumpkin-spice-martini: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "death-by-chocolate-frappe: no tags (no data-seasons/data-profiles)",
  "death-by-chocolate-frappe: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "death-by-chocolate-frappe: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "candy-corn-cream-soda: empty madeWith (data-lines)",
  "candy-corn-cream-soda: no tags (no data-seasons/data-profiles)",
  "candy-corn-cream-soda: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "candy-corn-cream-soda: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "stirling-bulldog: no tags (no data-seasons/data-profiles)",
  "stirling-bulldog: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "stirling-bulldog: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "bubblicious-martini: no tags (no data-seasons/data-profiles)",
  "bubblicious-martini: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "bubblicious-martini: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "hot-rum-coffee: no tags (no data-seasons/data-profiles)",
  "hot-rum-coffee: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "hot-rum-coffee: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "cappuccino-royale: empty madeWith (data-lines)",
  "cappuccino-royale: no tags (no data-seasons/data-profiles)",
  "cappuccino-royale: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "cappuccino-royale: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "royal-coffee-martini: no tags (no data-seasons/data-profiles)",
  "royal-coffee-martini: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "royal-coffee-martini: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "toasted-almond-latte: no tags (no data-seasons/data-profiles)",
  "toasted-almond-latte: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "toasted-almond-latte: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "stirling-mulled-cider: no tags (no data-seasons/data-profiles)",
  "stirling-mulled-cider: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "stirling-mulled-cider: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "brandy-collins: empty madeWith (data-lines)",
  "brandy-collins: no tags (no data-seasons/data-profiles)",
  "brandy-collins: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "brandy-collins: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "irish-cream-non-dairy-coffee-creamer: no tags (no data-seasons/data-profiles)",
  "irish-cream-non-dairy-coffee-creamer: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "irish-cream-non-dairy-coffee-creamer: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "hot-buttered-whiskey: no tags (no data-seasons/data-profiles)",
  "hot-buttered-whiskey: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "hot-buttered-whiskey: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "coffee-nudge: no tags (no data-seasons/data-profiles)",
  "coffee-nudge: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "coffee-nudge: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "pomegranate-mule: no tags (no data-seasons/data-profiles)",
  "pomegranate-mule: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "pomegranate-mule: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "toffee-macchiato: no tags (no data-seasons/data-profiles)",
  "toffee-macchiato: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "toffee-macchiato: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "creme-cream-soda: empty madeWith (data-lines)",
  "creme-cream-soda: no tags (no data-seasons/data-profiles)",
  "creme-cream-soda: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "creme-cream-soda: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "snickerdoodle-latte: no tags (no data-seasons/data-profiles)",
  "snickerdoodle-latte: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "snickerdoodle-latte: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "salted-caramel-cocoa: empty madeWith (data-lines)",
  "salted-caramel-cocoa: no tags (no data-seasons/data-profiles)",
  "salted-caramel-cocoa: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "salted-caramel-cocoa: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "lavender-white-chocolate-martini: no tags (no data-seasons/data-profiles)",
  "lavender-white-chocolate-martini: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "lavender-white-chocolate-martini: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "chocolate-mint-mocha-frappe: empty madeWith (data-lines)",
  "chocolate-mint-mocha-frappe: no tags (no data-seasons/data-profiles)",
  "chocolate-mint-mocha-frappe: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "chocolate-mint-mocha-frappe: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "stirling-mulled-wine: no tags (no data-seasons/data-profiles)",
  "stirling-mulled-wine: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "stirling-mulled-wine: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "tiramisu-frappe: no tags (no data-seasons/data-profiles)",
  "tiramisu-frappe: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "tiramisu-frappe: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "gingerbread-white-mocha: no tags (no data-seasons/data-profiles)",
  "gingerbread-white-mocha: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "gingerbread-white-mocha: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "candy-cane-cocktail: empty madeWith (data-lines)",
  "candy-cane-cocktail: no tags (no data-seasons/data-profiles)",
  "candy-cane-cocktail: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "candy-cane-cocktail: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null.",
  "frozen-gingerbread-eggnog: no tags (no data-seasons/data-profiles)",
  "frozen-gingerbread-eggnog: no \"Swap it\" note present on the page (legitimately absent, not an extraction miss) — stored as null.",
  "frozen-gingerbread-eggnog: no \"Spike it\" note present on the page (legitimately absent — either already alcoholic or not applicable) — stored as null."
]